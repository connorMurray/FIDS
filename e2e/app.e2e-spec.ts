import { FIDSPage } from './app.po';

describe('fids App', () => {
  let page: FIDSPage;

  beforeEach(() => {
    page = new FIDSPage();
  });

  // it('should display message saying app works', () => {
  //   page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('app works!');
  // });
});
